apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dm-stream-processor
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/nis94/dream-mobility.git
    targetRevision: main
    path: deploy/helm/stream-processor
    helm:
      releaseName: dm-stream-processor
      valueFiles:
        - values.yaml
        - values-kind.yaml
      parameters:
        - name: image.tag
          value: latest
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
