name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  go-lint:
    name: Go lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: true
      - name: golangci-lint
        # v7+ of the action installs golangci-lint v2.x which understands the
        # v2 config format (.golangci.yml). v6 capped at v1 and rejected v2
        # top-level keys like `formatters` and `version`.
        uses: golangci/golangci-lint-action@v7
        with:
          version: latest
          args: --timeout=5m

  go-test:
    name: Go unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: true
      - run: go build ./...
      - run: go test -race -count=1 ./...

  python-lint:
    name: Python lint (generator)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
      - name: uv sync
        working-directory: tools/generator
        run: uv sync --all-extras --dev
      - name: ruff
        working-directory: tools/generator
        run: uv run ruff check .

  compose-validate:
    name: docker-compose config validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker compose -f deploy/docker-compose.yml config -q
